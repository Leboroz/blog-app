<h1>New Post</h1>

<%= form_with model: @new_post, url: user_posts_url(params[:user_id]), scope: :post do |form|%>
  <%= form.label :title %>
  <%= form.text_field :title %><br>
  <%= form.label :text %>
  <%= form.text_area :text %>
  <%= form.submit 'Create'%>
<% end %>

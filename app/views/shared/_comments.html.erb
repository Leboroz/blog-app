<%= form_with model: @new_comment, url: user_post_comments_url(current_user.id, params[:id]), scope: :comment do |form| %>
  <%= form.label :text %>
  <%= form.text_area :text %>
  <%= form.submit 'Create' %>
<% end %>
